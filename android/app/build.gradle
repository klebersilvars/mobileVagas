defaultConfig {
    applicationId "com.kleberfjv.mobilevagas" // <- esse é o importante
    minSdkVersion rootProject.ext.minSdkVersion
    targetSdkVersion rootProject.ext.targetSdkVersion
    versionCode 1
    versionName "1.0.0"
}
